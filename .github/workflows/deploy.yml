
name: update_service_provider

on:
  push:
    branches: [ master ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: update on service provider
        run: curl https://unn4m3dd.xyz/update?repo=micro-site-ams --connect-timeout 300
